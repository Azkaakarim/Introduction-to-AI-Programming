## Lab 5: Frequently Used Methods  

This folder contains the lab work for **Lab 5**, focusing on Python's commonly used built-in methods to simplify data handling in engineering applications.

### Topics Covered
- `enumerate()`  
- `zip()`  
- `map()`  
- `filter()`  
- `sorted()`  
- `reversed()`  
- `range()`  
- `any()`  
- `all()`  
- `isinstance()`  
- `type()`  
- `len()`  
- `sum()`  
- `max()`  
- `min()`  
- `abs()`  
- `round()`  
- `divmod()`  
- `pow()`  

### Objective
To develop practical skills in using Python’s built-in functions for data processing, measurement analysis, and computational tasks, enabling students to write cleaner and more efficient code for engineering and AI-related problems.
