# Assignment 2 – Introduction to AI Programming

This folder contains **Assignment 2** of the course **Introduction to AI Programming**. The assignment focuses on loading CSV data, organizing it into separate lists, and performing manual grouping using Python.

## Assignment Tasks

- Load the CSV file manually using the `csv` module.  
- Store columns separately: `days`, `hours`, `consumptions`.  
- Group consumption by hour into a dictionary.

## Implementation Summary

- Loaded the CSV file manually using the `csv` module.  
- Stored each column (`days`, `hours`, `consumptions`) in separate lists.  
- Grouped consumption by hour into a dictionary for easy data access and analysis.  

## Purpose

The purpose of this assignment is to:

- Practice manually handling CSV data in Python.  
- Learn to organize data into separate lists.  
- Apply manual grouping techniques using dictionaries.  
- Reinforce skills in data manipulation and structured programming.
